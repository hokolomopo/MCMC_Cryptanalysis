function plot_Q_1_1_2

plot_size = 10;

t = 1:plot_size;

prob_eq = zeros(plot_size, 1);
prob_c = zeros(plot_size, 1);

for i = 1:plot_size
    [prob_eq(i, 1), prob_c(i), ~] = estimate_prob(i);
end

plot(t, prob_eq);
hold on;
plot(t, prob_c);

end